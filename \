#include "gradientMex.cpp"
#include <opencv2/core/core.hpp>
#include <opencv2/highgui/highgui.hpp>
#include <opencv2/imgproc/imgproc.hpp>
#include <stdio.h>

int main(){
    cv::Mat img = cv::imread("imgs/img/1.jpg", 0);
    img.convertTo(img, CV_32FC1);

    cv::Size orig_size = img.size();
    cv::imshow("1", img);
    cv::waitKey();
    float *buf1 = (float*)img.data;//new float[orig_size.height * orig_size.width];
    cv::imshow("2", img);
    cv::waitKey();
    //img.put(0,0,buf1);
    printf("End\n");
}
